{
  "name": "zonix",
  "version": "1.0.0",
  "scripts": {
    "build:web": "echo 'web build placeholder (plain html)'; exit 0",
    "prepare:capacitor": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
